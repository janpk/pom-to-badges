name: 'Pom To Badges'
description: 'A simple action that takes a list of maven pom dependencies to create version badges for'
inputs:
  github-token:
    description: 'GitHub token'
    required: false
runs:
  using: 'docker'
  image: 'docker://ghcr.io/janpk/pom-to-badges.git:main'
